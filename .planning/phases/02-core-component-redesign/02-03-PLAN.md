---
phase: 02-core-component-redesign
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [css/monochrome-components.css]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Button components follow monochrome design"
    - "Card components have clean borders"
    - "Form inputs have minimalist styling"
    - "All components have zero decorations"
  artifacts:
    - path: "css/monochrome-components.css"
      provides: "Complete component library"
      min_lines: 250
  key_links:
    - from: "css/input.css"
      to: "css/monochrome-components.css"
      via: "@import directive"
      pattern: "@import.*monochrome-components"
---

<objective>
Create comprehensive button, card, and form component classes

Purpose: Complete the minimalist component library for all UI elements
Output: Full set of reusable component classes following the design system

Note: This phase addresses roadmap tasks 2.3-2.5 (button components, card components, form styling)
</objective>

<execution_context>
@/Users/maulik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maulik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-SYSTEM.md
@css/design-tokens.css
@.planning/phases/02-core-component-redesign/02-01-SUMMARY.md
@.planning/phases/02-core-component-redesign/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive button components</name>
  <files>css/monochrome-components.css</files>
  <read_first>
    css/monochrome-components.css
    css/design-tokens.css
    .planning/DESIGN-SYSTEM.md
  </read_first>
  <action>
    Append comprehensive button component classes to monochrome-components.css:

    ```css
    /* Button Components */

    /* Primary Button - Black background, white text */
    .btn-primary {
      background: var(--color-black);
      color: var(--color-white);
      border: 2px solid var(--color-black);
      padding: 12px 24px;
      font-family: var(--font-ui);
      font-size: 16px;
      font-weight: 600;
      min-height: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      border-radius: 0;
      box-shadow: none;
      transition: none;
      text-decoration: none;
    }

    .btn-primary:hover {
      background: var(--color-white);
      color: var(--color-black);
    }

    /* Secondary Button - White background, black text */
    .btn-secondary {
      background: var(--color-white);
      color: var(--color-black);
      border: 2px solid var(--color-black);
      padding: 12px 24px;
      font-family: var(--font-ui);
      font-size: 16px;
      font-weight: 600;
      min-height: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      border-radius: 0;
      box-shadow: none;
      transition: none;
      text-decoration: none;
    }

    .btn-secondary:hover {
      background: var(--color-black);
      color: var(--color-white);
    }

    /* Accent Button - Brand orange for CTAs */
    .btn-accent {
      background: #FF9900;
      color: var(--color-white);
      border: 2px solid #FF9900;
      padding: 12px 24px;
      font-family: var(--font-ui);
      font-size: 16px;
      font-weight: 600;
      min-height: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      border-radius: 0;
      box-shadow: none;
      transition: none;
      text-decoration: none;
    }

    .btn-accent:hover {
      background: var(--color-black);
      border-color: var(--color-black);
    }

    /* Link Button - Text only with underline */
    .btn-link {
      background: transparent;
      color: #1E3A5F;
      border: none;
      padding: 8px 16px;
      font-family: var(--font-ui);
      font-size: 16px;
      font-weight: 500;
      text-decoration: underline;
      cursor: pointer;
      transition: none;
    }

    .btn-link:hover {
      color: var(--color-black);
    }

    /* Button Sizes */
    .btn-sm {
      padding: 8px 16px;
      font-size: 14px;
      min-height: 36px;
    }

    .btn-lg {
      padding: 16px 32px;
      font-size: 18px;
      min-height: 52px;
    }

    /* Button States */
    .btn-disabled,
    button:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }
    ```

    Ensure all buttons have:
    - border-radius: 0 (sharp corners)
    - box-shadow: none (no shadows)
    - transition: none (instant state changes)
    - min-height: 44px for accessibility (except small variant)
  </action>
  <acceptance_criteria>
    - File contains ".btn-primary" with "background: var(--color-black)"
    - File contains ".btn-secondary" with "border: 2px solid"
    - File contains ".btn-accent" with "background: #FF9900"
    - File contains ".btn-link" with "text-decoration: underline"
    - File contains "border-radius: 0" in all button classes
    - File contains "min-height: 44px" for standard buttons
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\.btn-" css/monochrome-components.css && grep -c "border-radius: 0" css/monochrome-components.css</automated>
  </verify>
  <done>Complete button component library added with all variants (roadmap task 2.3)</done>
</task>

<task type="auto">
  <name>Task 2: Add card and form components</name>
  <files>css/monochrome-components.css</files>
  <read_first>
    css/monochrome-components.css
    css/design-tokens.css
    .planning/DESIGN-SYSTEM.md
  </read_first>
  <action>
    Append card and form component classes to monochrome-components.css:

    ```css
    /* Card Components */

    .card {
      background: var(--color-white);
      border: 1px solid var(--color-black);
      padding: 32px;
      border-radius: 0;
      box-shadow: none;
    }

    .card-hover:hover {
      border-width: 2px;
      margin: -1px; /* Compensate for border width change */
    }

    .card-header {
      margin-bottom: 24px;
      padding-bottom: 16px;
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    .card-title {
      font-family: var(--font-headline);
      font-size: 24px;
      font-weight: 600;
      color: var(--color-black);
      margin: 0;
    }

    .card-body {
      font-family: var(--font-body);
      font-size: 16px;
      line-height: 1.7;
      color: var(--color-black);
    }

    .card-footer {
      margin-top: 24px;
      padding-top: 16px;
      border-top: 1px solid rgba(0, 0, 0, 0.1);
    }

    /* Form Components */

    .form-group {
      margin-bottom: 24px;
    }

    .form-label {
      display: block;
      font-family: var(--font-ui);
      font-size: 14px;
      font-weight: 600;
      color: var(--color-black);
      margin-bottom: 8px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .form-input,
    .form-textarea,
    .form-select {
      width: 100%;
      background: var(--color-white);
      border: 1px solid var(--color-black);
      padding: 12px 16px;
      font-family: var(--font-ui);
      font-size: 16px;
      color: var(--color-black);
      border-radius: 0;
      box-shadow: none;
      transition: none;
      min-height: 44px;
    }

    .form-textarea {
      min-height: 120px;
      resize: vertical;
    }

    .form-input:focus,
    .form-textarea:focus,
    .form-select:focus {
      outline: none;
      border-width: 2px;
      margin: -1px; /* Compensate for border width */
    }

    .form-input::placeholder,
    .form-textarea::placeholder {
      color: rgba(0, 0, 0, 0.4);
    }

    /* Form States */
    .form-error {
      border-color: #DC2626;
      color: #DC2626;
    }

    .form-success {
      border-color: #059669;
      color: #059669;
    }

    .form-help {
      font-family: var(--font-ui);
      font-size: 14px;
      color: rgba(0, 0, 0, 0.6);
      margin-top: 4px;
    }

    .form-error-message {
      font-family: var(--font-ui);
      font-size: 14px;
      color: #DC2626;
      margin-top: 4px;
    }

    /* Checkbox and Radio */
    .form-checkbox,
    .form-radio {
      width: 20px;
      height: 20px;
      border: 2px solid var(--color-black);
      cursor: pointer;
      margin-right: 8px;
    }

    .form-checkbox {
      border-radius: 0;
    }

    .form-radio {
      border-radius: 50%; /* Exception for radio buttons */
    }
    ```

    All components maintain minimalist aesthetic:
    - Sharp corners (border-radius: 0) except radio buttons
    - No shadows (box-shadow: none)
    - Instant interactions (transition: none)
    - Clean 1px or 2px borders
  </action>
  <acceptance_criteria>
    - File contains ".card" with "border: 1px solid"
    - File contains ".form-input" with "border-radius: 0"
    - File contains ".form-label" with "text-transform: uppercase"
    - File contains ".form-error" with "border-color: #DC2626"
    - File contains ".form-checkbox" with "border-radius: 0"
    - All form elements have "min-height: 44px"
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\.form-" css/monochrome-components.css && grep -c "\.card" css/monochrome-components.css</automated>
  </verify>
  <done>Card and form components added with minimalist styling (roadmap tasks 2.4 and 2.5)</done>
</task>

</tasks>

<verification>
- Verify monochrome component classes are imported via css/input.css
- Test button hover states (instant color inversion)
- Test form focus states (2px border)
- Test card hover states (thicker border)
- Check that all buttons meet 44px minimum height for accessibility
- Verify form inputs are properly styled with sharp corners
</verification>

<success_criteria>
- Complete component library with buttons, cards, and forms
- All components follow zero-decoration principle
- Touch-friendly sizing (44px minimum)
- Instant interactions throughout
- Ready for use across all pages
- Addresses roadmap tasks 2.3 (buttons), 2.4 (cards), and 2.5 (forms)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-component-redesign/02-03-SUMMARY.md`
</output>