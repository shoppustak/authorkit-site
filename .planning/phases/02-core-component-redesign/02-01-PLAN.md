---
phase: 02-core-component-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [includes/header.html, css/monochrome-components.css, js/header-loader.js]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Header displays with pure black/white design"
    - "Navigation links are visible and clickable"
    - "Mobile menu toggles correctly"
    - "Sticky header behavior works"
  artifacts:
    - path: "includes/header.html"
      provides: "Minimalist header component"
      min_lines: 40
    - path: "css/monochrome-components.css"
      provides: "Component utility classes"
      contains: ".nav-"
  key_links:
    - from: "includes/header.html"
      to: "css/monochrome-components.css"
      via: "class attributes"
      pattern: "class.*nav-"
    - from: "js/header-loader.js"
      to: "mobile menu toggle"
      via: "event listener"
      pattern: "getElementById.*mobile-menu"
---

<objective>
Redesign the header component with Minimalist Monochrome aesthetic

Purpose: Transform the header navigation to pure black/white design with zero decorations
Output: Redesigned header.html with monochrome styling and instant interactions
</objective>

<execution_context>
@/Users/maulik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maulik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-SYSTEM.md
@css/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monochrome component CSS file</name>
  <files>css/monochrome-components.css</files>
  <read_first>
    css/design-tokens.css
    css/style.css
    .planning/DESIGN-SYSTEM.md
  </read_first>
  <action>
    Create a new CSS file with utility classes for minimalist components. Include:

    1. Import the design tokens at the top: @import './design-tokens.css';

    2. Navigation classes:
    - .nav-container: max-width 1280px, padding 0 16px mobile/32px desktop, margin 0 auto
    - .nav-bar: background white, border-bottom 1px solid black, position sticky top 0, z-index 50, height 64px
    - .nav-link: color black, font-family var(--font-ui), font-size 16px, font-weight 500, no underline default, underline on hover
    - .nav-link-active: border-bottom 2px solid black
    - .nav-logo: height 40px, width auto

    3. Mobile menu classes:
    - .mobile-menu: position absolute, top 100%, right 16px, background white, border 1px solid black, display none (shown via JS)
    - .mobile-menu-button: padding 8px, background transparent, border none, cursor pointer
    - .mobile-menu-icon: width 24px, height 24px, stroke black, stroke-width 2
    - .mobile-nav-link: display block, padding 16px 24px, color black, border-bottom 1px solid rgba(0,0,0,0.1)

    4. Button classes (header CTA):
    - .btn-primary: background black, color white, padding 12px 24px, border 2px solid black, font-weight 600, min-height 44px, cursor pointer
    - .btn-primary:hover: background white, color black (instant color inversion)

    All classes must:
    - Use border-radius: 0 (no rounded corners)
    - Use box-shadow: none (no shadows)
    - Use transition: none (instant interactions)
    - Reference CSS custom properties from design-tokens.css
  </action>
  <acceptance_criteria>
    - File contains "@import './design-tokens.css';"
    - File contains ".nav-bar" with "border-bottom: 1px solid"
    - File contains ".nav-link" with "color: black" or "color: var(--color-black)"
    - File contains ".btn-primary" with "background: black" or "background: var(--color-black)"
    - File contains "border-radius: 0" at least 3 times
    - File contains "box-shadow: none" at least 2 times
    - File contains "transition: none" at least 2 times
  </acceptance_criteria>
  <verify>
    <automated>grep -c "border-radius: 0" css/monochrome-components.css && grep -c "box-shadow: none" css/monochrome-components.css</automated>
  </verify>
  <done>CSS file created with minimalist component classes using design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Redesign header HTML structure</name>
  <files>includes/header.html</files>
  <read_first>
    includes/header.html
    css/monochrome-components.css
    .planning/DESIGN-SYSTEM.md
  </read_first>
  <action>
    Transform the header.html to minimalist monochrome design:

    1. Replace the outer nav element:
    ```html
    <nav class="nav-bar">
        <div class="nav-container">
    ```

    2. Update logo section (keep existing logo, just update container):
    ```html
    <div class="flex items-center">
        <a href="index.html" class="flex items-center">
            <img src="images/logo-authorkit.svg" alt="AuthorKit" class="nav-logo">
        </a>
    </div>
    ```

    3. Update desktop navigation links - remove all Tailwind decoration classes:
    ```html
    <div class="hidden lg:flex items-center space-x-8">
        <a href="index.html" class="nav-link" data-page="home">Home</a>
        <a href="features.html" class="nav-link" data-page="features">Features</a>
        <a href="pricing.html" class="nav-link" data-page="pricing">Pricing</a>
        <a href="https://bookshelf.authorkit.pro" class="nav-link" target="_blank">Bookshelf</a>
        <a href="blog.html" class="nav-link" data-page="blog">Blog</a>
        <a href="docs.html" class="nav-link" data-page="docs">Docs</a>
        <a href="download.html" class="btn-primary">Download Free</a>
    </div>
    ```

    4. Update mobile menu button - remove rounded and transition classes:
    ```html
    <button type="button" class="mobile-menu-button" id="mobile-menu-button" aria-label="Toggle menu">
        <svg class="mobile-menu-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="square" stroke-linejoin="miter" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
    </button>
    ```

    5. Update mobile menu container - remove all shadow and rounded classes:
    ```html
    <div class="mobile-menu hidden" id="mobile-menu">
        <a href="index.html" class="mobile-nav-link" data-page="home">Home</a>
        <a href="features.html" class="mobile-nav-link" data-page="features">Features</a>
        <a href="pricing.html" class="mobile-nav-link" data-page="pricing">Pricing</a>
        <a href="https://bookshelf.authorkit.pro" class="mobile-nav-link" target="_blank">Bookshelf</a>
        <a href="blog.html" class="mobile-nav-link" data-page="blog">Blog</a>
        <a href="docs.html" class="mobile-nav-link" data-page="docs">Docs</a>
        <a href="download.html" class="mobile-nav-link">Download Free</a>
    </div>
    ```

    Key changes:
    - Remove ALL rounded classes (rounded-lg, rounded-xl)
    - Remove ALL shadow classes (shadow-sm, shadow-xl)
    - Remove ALL transition classes (transition, transition-colors)
    - Remove ALL color classes that aren't pure black/white (text-gray-600, hover:text-blue-600, bg-blue-600, etc.)
    - Keep structural Tailwind classes (flex, hidden, lg:flex, items-center, space-x-8)
    - Use new monochrome component classes from css/monochrome-components.css
  </action>
  <acceptance_criteria>
    - File contains 'class="nav-bar"'
    - File contains 'class="nav-link"' at least 6 times
    - File contains 'class="btn-primary"'
    - File does NOT contain "rounded" anywhere
    - File does NOT contain "shadow" anywhere
    - File does NOT contain "transition" anywhere
    - File does NOT contain "text-gray" anywhere
    - File does NOT contain "bg-blue" anywhere
  </acceptance_criteria>
  <verify>
    <automated>grep -c "nav-link" includes/header.html && ! grep -q "rounded\|shadow\|transition" includes/header.html</automated>
  </verify>
  <done>Header HTML redesigned with minimalist monochrome classes and zero decorations</done>
</task>

<task type="auto">
  <name>Task 3: Update header loader JavaScript</name>
  <files>js/header-loader.js</files>
  <read_first>
    js/header-loader.js
    includes/header.html
  </read_first>
  <action>
    Update the header-loader.js to ensure compatibility with redesigned header:

    1. Keep the existing fetch and injection logic intact

    2. Update the mobile menu toggle logic to use new class names:
    - Look for getElementById('mobile-menu-button')
    - Look for getElementById('mobile-menu')
    - Update the toggle logic to use classList.toggle('hidden') without any transition delays

    3. Update the active page highlighting:
    - Look for querySelectorAll('.nav-link') instead of any old selector
    - Add 'nav-link-active' class to matching link instead of old active classes

    4. Ensure instant interactions:
    - Remove any setTimeout or transition-related delays if present
    - Make all toggles and class changes instant (no animation frames)

    The mobile menu should toggle instantly between hidden and visible states with no slide or fade effects.
  </action>
  <acceptance_criteria>
    - File contains "querySelectorAll('.nav-link')"
    - File contains "classList.add('nav-link-active')" or similar active class logic
    - File contains "classList.toggle('hidden')" for mobile menu
    - File does NOT contain "setTimeout" related to UI changes
    - File does NOT contain "transition" in any animation context
  </acceptance_criteria>
  <verify>
    <automated>grep -q "nav-link" js/header-loader.js && grep -q "classList.toggle" js/header-loader.js</automated>
  </verify>
  <done>Header loader JavaScript updated for instant minimalist interactions</done>
</task>

</tasks>

<verification>
- Load any page and verify header displays with pure black/white design
- Click navigation links and verify instant hover states (underline appears)
- Click mobile menu button and verify instant toggle (no slide animation)
- Resize browser and verify responsive breakpoints work
- Check sticky header behavior on scroll
</verification>

<success_criteria>
- Header follows Minimalist Monochrome design system
- Zero border-radius throughout (no rounded corners)
- Zero shadows throughout
- Instant interactions (no transitions)
- Mobile menu works without animations
- All navigation links functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-component-redesign/02-01-SUMMARY.md`
</output>